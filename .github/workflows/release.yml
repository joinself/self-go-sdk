name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Git tag to release e.g. 1.2.3-4"
        type: string
        required: true

jobs:
  release:
    permissions:
      contents: write
    uses: joinself/workflows-public/.github/workflows/release.yml@main
    with:
      version: ${{ inputs.version }}
    secrets: inherit
